{
  "name": "repo-sweep",
  "version": "1.0.0",
  "description": "Clean build artifacts from project directories before transferring",
  "type": "module",
  "bin": {
    "repo-sweep": "./dist/index.js"
  },
  "scripts": {
    "start": "npx tsx src/index.ts",
    "build": "tsc",
    "sweep": "npx tsx src/index.ts"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0"
  }
}
